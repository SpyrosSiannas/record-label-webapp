<div class="col-md">
    <div class="artist-info" onclick="redirectToArtist({{artist_id}})">
        <img src="{{profile_img}}">
        <div class="overlay-main"></div>
        <div class="d-flex overlay-content flex-column align-items-end">
            <div class="spotify-container mt-auto">
                <a href="{{spotify_url}}">
                <img src="/img/listen-on-spotify-1.png">
                </a>
            </div>
            <p>{{name}}</p>
        </div>
    </div>
</div>